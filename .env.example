# Database configuration (for local PostgreSQL - docker-compose.yml)
POSTGRES_DB=creatorledger
POSTGRES_USER=creatorledger
POSTGRES_PASSWORD=change-me-in-production

# Database configuration (for external DB - docker-compose.external-db.yml)
# Example for Neon: postgresql://username:password@ep-xxx.eu-central-1.aws.neon.tech/creatorledger?sslmode=require
SPRING_DATASOURCE_URL=postgresql://localhost:5432/creatorledger
SPRING_DATASOURCE_USERNAME=creatorledger
SPRING_DATASOURCE_PASSWORD=change-me-in-production

# Application configuration
SPRING_PROFILES_ACTIVE=prod
APP_PORT=8080

# Docker image (set by CI/CD)
GITHUB_REPOSITORY=robbiemcarthur/creator-ledger
IMAGE_TAG=latest
