spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/creatorledger_dev
    username: creatorledger
    password: creatorledger
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration